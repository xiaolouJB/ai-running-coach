# ── 个人课表 & 打卡图（只保留示例文件）──
# 包含个人数据的文件：*_schedule.md / *_weekly_checkin_*.html 等均被此规则覆盖
schedules/*
!schedules/example_schedule.md
!schedules/.gitkeep

# ── 个人训练原始数据（.fit 文件）──
训练数据/

# ── 操作系统产生的临时文件 ──
.DS_Store
Thumbs.db
desktop.ini

# ── 编辑器临时文件 ──
.vscode/
.idea/
*.swp
*.swo

# ── 版权内容 - 禁止公开（书籍原文及衍生物）──
# 整个 理论/ 目录一律不进仓库：不只是抽取产物，连抽取脚本本身也含书籍文件名与本地路径。
# （曾差点把 理论/extract_heat.py、理论/fix_nutrition_chunks.py 提交进公开仓库）
理论/

# ── 认证凭证 ──
_encoded_cookies.txt
*_token*
*credential*
.env*
*cookies*

# ── 个人训练数据产物（parse_fit.py 的输出与本地分析脚本）──
# 这些是「你自己的」跑步记录，绝不应进公开仓库
runs.csv
all_runs.csv
*_runs.csv
workouts/
detailed_analysis.py

# ── Python 缓存 ──
__pycache__/
*.pyc
*.pyo
