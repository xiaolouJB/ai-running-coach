# 贡献指南

感谢你对 AI Running Coach 的关注！本文件说明如何为项目贡献适配器、训练理论和其他内容。

---

## 一、适配器贡献规范

适配器让 AI 教练核心逻辑能与不同品牌手表配合工作。如果你熟悉某款手表的 MCP 接入方式，欢迎贡献适配器。

### 1.1 前提条件

- 该设备有可用的 MCP 服务端点（或可以通过现有 API 包装成 MCP）
- 你已验证至少以下 4 个核心接口可用：`GET_SPORT_RECORDS`、`GET_RESTING_HR`、`GET_RECOVERY_STATUS`、`GET_FITNESS_OVERVIEW`
- 接口返回的数据字段与 ADAPTERS.md 中的字段说明可以对应

### 1.2 适配器格式

在 ADAPTERS.md 末尾追加以下格式的新适配器章节：

```markdown
## N、[设备品牌名称]适配器

**状态**：🚧 开发中 / ✅ 正式支持
**MCP 端点**：[MCP 服务 URL]
**认证方式**：[OAuth 2.0 / API Key / 其他]
**支持的设备型号**：[例如：Forerunner 系列、Fenix 系列]

### N.1 工具映射表

| 抽象接口 | [设备] MCP 工具名 | 关键参数 | 说明 |
|---------|----------------|---------|------|
| `GET_SPORT_RECORDS` | `mcp__[设备]__[工具名]` | `startDate`, `endDate` | 说明 |
| `GET_RESTING_HR` | `mcp__[设备]__[工具名]` | `startDate`, `endDate` | 说明 |
...

### N.2 字段差异说明

如果某设备的字段名或单位与 COROS 基准不同，在此说明归一化方式：

| 差异项 | [设备]原始值 | 归一化为 | 处理方式 |
|--------|-----------|--------|---------|
| [例如配速单位] | min/mile | sec/km | × 37.28 |
```

### 1.3 测试要求

提交适配器前，请验证：
- [ ] 能成功拉取最近 7 天的运动记录
- [ ] VO2max / 体能评估字段返回非空值
- [ ] 恢复状态字段返回合理数值
- [ ] MCP 连接说明在 USER_GUIDE.md 的安装步骤基础上可以复用

---

## 二、训练理论贡献规范

THEORY_LIBRARY.md 第四章是开放的用户自定义区，任何人可以追加训练理论笔记。如果你想将理论内容提交到主库（第一至三章），需要满足以下条件。

### 2.1 理论内容要求

- **有明确出处**：需引用具体书籍、论文或被广泛认可的教练体系
- **有 AI 使用规则**：需将理论转化为 AI 可执行的判断条件（格式参见第一章 1.3 节）
- **不与现有理论矛盾**：如有冲突，需在文中说明处理优先级

### 2.2 格式模板

```markdown
## 第N章：[理论名称]

**出处**：[作者], *[书名/论文名]* ([年份]), [出版社].
**核心贡献**：[一句话说明该理论的核心贡献]

### N.1 [理论要点标题]

[理论内容说明]

### N.2 AI 使用规则

- [条件] → [AI 执行的动作]
- [条件] → [AI 执行的动作]
```

---

## 三、提交前密钥自检

为防止敏感信息（如 Token、Cookie、API Key、个人绝对路径）误提交到公开仓库，请在 `git commit` 前执行以下命令自检：

```bash
git diff --staged | grep -Ein "token|secret|api[_-]?key|password|cookie|c:\\\\Users"
```

如果命令返回任何匹配结果，请务必将其脱敏或从暂存区中移除后再行提交。示例凭证一律使用 `<YOUR_TOKEN>` 占位符。

---

## 四、架构演进：为何不再使用向量数据库

在 v2.0 及更早版本中，系统曾通过 `rebuild_vectordb.py` 尝试构建向量数据库以实现理论知识的检索。但在 v2.1 架构升级中，我们全面转向了**分层结构化知识卡片库（`knowledge_cards/`）与规则路由机制**。

主要考量如下：
1. **可靠性与确定性**：向量检索在专业运动科学与伤病诊断场景下容易出现检索漂移或幻觉，而结构化卡片能保证 AI 精准触发特定的诊断与康复规则。
2. **免外挂依赖**：去除了向量数据库与嵌入模型依赖，降低了环境搭建门槛，使得全新克隆项目后开箱即用。
3. **版权合规**：不再依赖版权图书全文抽取，知识卡片均为运动科学通识的自写表达，便于公开合规分发。

---

## 五、问题反馈

如果你遇到以下问题，欢迎反馈：
- AI 生成的配速区间与你实际感受严重不符
- 某个 MCP 工具调用持续失败
- 课表逻辑有明显错误（如恢复日被安排了高强度训练）

反馈时请附上：
1. 你的设备型号和 COROS App 版本
2. 具体的报错信息或 AI 输出内容
3. 你的期望行为

---

*AI Running Coach v2.1.1 · 欢迎 Issue 和 PR*
