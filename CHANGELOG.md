# 更新日志 · Changelog

本项目的所有重要变更记录于此。版本号遵循 `主版本.次版本.修订号`。

---

## [3.0.0] — 2026-07-29

力量与营养两块知识补齐到"看着就能练、照着就能补"的程度。

### 新增（Added）

- **15 个力量动作的完整动作库**：每个动作含目标肌群、器材档位（无器材 / 小器械 / 健身房）、三级阶梯剂量，以及爆发类、高离心、需基础期打底等特性标记。以动作 ID 为唯一权威源，课表只能从这 15 个里选，不会凭空造动作。
- **动作执行指引**：每个动作都有分步要领、常见错误（错在哪 + 怎么改）、跑者专项 cue（为什么这个动作对跑步有用）、呼吸配合。不用再自己去搜视频。
- **分期方案表**：基础期 / 强化期 / 维持期 / 减量期各自的频率、时长、负荷、动作节奏与推荐动作组合，与备赛周期确定性绑定。
- **动作偏好与替补**：可以拉黑做不了的动作，也可以声明手上有什么器材。**排除不等于少练**——被排掉的位置由同部位动作确定性替补，训练量不变；每个部位至少保留两个可用动作，保证任何阶段都排得出课。
- **`nutrition_calc.py` 营养算术脚本**：出汗率、跑中补液目标（含胃排空 800ml/h 封顶）、补钠、碳水、能量胶数量与时点、赛后补液，全部由脚本算好，**AI 只负责复述,不做乘除**。

### 变更（Changed）

- 原 4 张力量卡片转为索引页，指向新的动作库 / 动作指引 / 分期方案三件套，原有的触发关键词保持有效。
- 营养卡片补上数字口径出处，明确哪些数值来自脚本计算。
- 课表生成审查新增一条：力量动作必须在库内且不得命中排除集。

---

## [2.3.0] — 2026-07-29

把「真实世界的变量」纳入判定：睡不好、桑拿天、生理周期、伤病，教练现在都会算进去。

### 新增（Added）

- **每日身体准备度（多信号红旗否决）**：不再只看 HRV 或静息心率单指标。HRV 跌幅、静息心率升幅、睡眠分三项各记一面红旗，0 面正常 / 1 面提示 / 2 面以上换恢复日。**缺测的信号不参与判定也不计红旗**，避免因少一项数据就误报警。三项全缺时明确告知「未做状态判定」，不会假装一切正常。新增 `readiness_calc.py` 供确定性计算。
- **环境修正（体感温度与湿度）**：体感 26/30/35℃ 三档分级，湿度 ≥80% 自动抬级。关键的是**按课型分叉**——关键质量课优先挪到清晨傍晚或跑步机把强度做完（而不是一热就砍量）；长跑保时长、只放慢；轻松跑随便慢，不影响效果。
- **周期感知（女性用户）**：按生理相位给训练弹性倾向，经期与黄体期后期不硬上强度。**默认关闭**，需显式开启，且要求性别与设备数据同时满足才生效；**不做任何医学判断，不预测经期**。
- **分段评估**：单次复盘可按圈（lap）分析——自动区分热身/正式/间歇/放松，给出配速变异系数与前后半程掉速，间歇课终于能说清「第几组开始掉」。

### 变更（Changed）

- **周期切分改为确定性规则**：按总周数四档（≤6 / 7-12 / 13-20 / >20 周）分配基础、强化、巅峰、减量四期比例，减量期固定 2-3 周。同样的输入每次得到同样的分期。
- **伤病降级补齐三条硬约束**：① 发作/恢复期强制冻结跑量递增；② 降级禁区为 `ban_zones ∪ {T,I,R}` —— 髂胫束、跑者膝、足底筋膜炎这类伤的禁区列表可能为空，此前会导致「完全不降速」；③ 改了距离必须同步重写当日文案，且单次跑量不低于 3km。

---

## [2.2.0] — 2026-07-29

设备适配层校准 + 反臆造准则。本版不新增训练知识，专注让文档里写的每一条「事实」都真实可用。

### 修正（Fixed）

- **HRV 工具名错误**：`queryHrvAssessment` 已被 COROS 官方删除，文档却仍指向它，导致 AI 拉取 HRV 失败。现统一改为 `querySleepHrv`。
- **VDOT 查表静默钳制**：`vdot_calculator.py` 此前对超出对照表范围的输入**静默返回最近边界值** —— 手滑把 5K 成绩填成 `8:00` 会安静得到一个离谱 VDOT，并据此生成整套错误配速且无人察觉。现在越界抛出明确报错，说明「输入值 / 支持范围 / 请核对输入」。表内断档取近似值时也会标注实际使用的 VDOT，不再静默。
- **主观感受（RPE）判断过时**：文档此前断言「COROS 只返回客观指标、不含 RPE」，因此每次复盘都要用户手动报分。实际上 `getActivityDetail` 已返回 `Perceived Effort` 字段。现改为**先读设备、读不到才问用户**。

### 新增（Added）

- **COROS MCP 工具映射表全量重写**：依据真实探测结果覆盖 22 个只读工具，每个标注「已验证 / 官方提供但未验证」，不再混淆。新增分段（`queryActivityLapData`）、压力（`queryStressLevel`）、月经周期（`queryMenstruationCycles`）、FIT 直链（`queryActivityFitFileDownloadUrls`）等此前缺失的工具。
- **SKILL.md 新增 S12「反臆造工程准则」**：接地优先于历史 / 确定性算术走脚本 / 查表越界必须报错 / 无数据不推断，四条。
- **无 MCP 手动模式正式成形**：`parse_fit.py` 接入设备适配文档，成为无手表或无 MCP 环境的官方入口。

### 变更（Changed）

- **训练负荷判定改用官方数值**：`queryTrainingLoadAssessment` 直接返回现成的 Load Ratio（即 ACWR），优先采用；无该值时才回落自算 ATL/CTL，两者不混用。
- **能力边界结论更新**：此前认为官方 MCP 只提供总和均值、不给分段与逐秒数据 —— 该结论已过时。分段逐圈数据、含 GPS 的 FIT 直链均已开放，`tools/list` 亦可枚举。
- **模型推荐改为能力档位**：不再点名具体模型版本（每隔数月即过期），改按「旗舰推理档 / 均衡档 / 轻量档」描述并标注最后核对日期。
- **明确课表回写仍不可行**：COROS 的写工具经实测确认未开放，文档不再留下可能被误解为「可写回手表」的表述。

---

## [2.1.1] — 2026-07-29

仓库瘦身。**本版不改变教练行为**，只移除仅作者本地环境能运行的脚本，让仓库对任何人克隆后都真正可用。

### 移除（Removed）

- `coros_api_test.py` —— 一次性接口探测脚本，依赖非公开接口，他人无法运行。
- `create_workouts.py` —— 依赖作者本地目录结构；且 COROS 并不支持导入 FIT 课表文件，该路线本身走不通。
- `rebuild_vectordb.py` —— 依赖因版权而不随仓库分发的目录，克隆后必然失败；且与 v2.1 起「结构化知识卡片取代向量检索」的架构方向相悖。
- `parse_fit_data.py` / `quick_summary.py` —— 与其他脚本重复，能力已并入 `parse_fit.py`。

### 新增 / 变更

- **`parse_fit.py`**：由原 `parse_fit_v2.py` 改造为标准命令行工具，支持 `--input` / `--out` / `--summary`，移除全部硬编码路径；缺依赖、缺参数、路径不存在、目录无 FIT 文件等情况均有明确提示与退出码；单个文件解析失败会跳过并继续处理其余。
- **`.gitignore` 加固**：凭证类文件、个人训练数据产物（`runs.csv` / `workouts/`）、以及含版权书籍信息的 `理论/` 整目录一律不进仓库。
- **`CONTRIBUTING.md`**：新增「提交前密钥自检」与「为何不再使用向量数据库」两节。

---

## [2.1.0] — 2026-06-03

- 新增分层知识卡片库（伤病 / 力量 / 营养共 14 张），由规则路由调用，取代外挂向量数据库。
- 扩充训练理论章节，去除对版权原文的依赖，实现全量合规分发。

## [2.0.0]

- 首个公开版本：三大训练理论驱动的课表生成、每周动态调整、赛前分析、周打卡图。
